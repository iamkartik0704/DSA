// find the number which appears only once - XOR operator
#include<bits/stdc++.h>
using namespace std;
class Solution {
public:
    int singleNumber(vector<int>& nums) {
            int j=0;
            int i=0;
            int count=0;
            for ( i = 0; i < nums.size(); i++)
            {
                if(nums[i]==nums[j]){
                    count++;

                }
                j++;
            }
            
    }
};
